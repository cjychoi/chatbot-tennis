# Create a bot via @BotFather on Telegram and paste the token here.
TELEGRAM_BOT_TOKEN="your-token-here"

# Optional: set to "1" to run Chromium with a visible window (for debugging).
# DEBUG_HEADFUL=1

# Optional: set to "1" to save a screenshot on scrape failure.
# DEBUG_SCREENSHOT=1
